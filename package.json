{
  "name": "pixel-whatsapp-tool",
  "version": "1.0.0",
  "description": "WhatsApp Bulk Messaging Workspace Root",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
